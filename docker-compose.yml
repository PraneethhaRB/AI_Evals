version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./results:/app/results

  dashboard:
    build: ./dashboard
    ports:
      - "5173:5173"
    depends_on:
      - api